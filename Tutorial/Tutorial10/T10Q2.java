package T10;

public class T10Q2 {
	/*
	 * Insertion sort repeatedly inserts an unsorted element into a sorted subarray
	 * 
	 * Selection sort repeatedly finds the smallest number in the list and put it
	 * from the left.
	 * 
	 * Bubble sort repeatedly compares successive neighboring pairs and swap them if
	 * the pair is in descending order
	 * 
	 * Merge sort repeatedly divides the array into two halves and merge them into
	 * sorted array
	 */
}
